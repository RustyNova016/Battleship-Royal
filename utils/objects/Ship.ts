export class Ship {
    id: string;
    name: string;
    length: string;


    constructor(id: string, name: string, length: string) {
        this.id = id;
        this.name = name;
        this.length = length;
    }
}